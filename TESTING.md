# 组件功能测试指南

**测试页面**: http://localhost:8002  
**启动时间**: 2026-04-07  
**测试状态**: ✅ 运行中

---

## Midscene 环境变量

如果你只运行基础 `Playwright` 冒烟用例，不需要额外配置 `.env`。

如果你想启用 `Midscene` 的 AI 断言能力：

1. 复制项目根目录的 `.env.example` 为 `.env`
2. 填入阿里百炼 API Key
3. 运行 `pnpm test:e2e`

阿里百炼 OpenAI 兼容模式示例：

```bash
MIDSCENE_MODEL_NAME=qwen-vl-max-latest
MIDSCENE_MODEL_API_KEY=your_dashscope_api_key
MIDSCENE_MODEL_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MIDSCENE_MODEL_FAMILY=qwen2.5-vl
```

---

## 🎯 测试内容

### 1. 树形结构管理

#### 展开/折叠节点
- ✅ 点击节点左侧的 ▶/▼ 按钮
- ✅ 验证节点可以展开/折叠
- ✅ 验证互斥展开（展开一个节点时，同层其他节点自动折叠）

#### 节点序号
- ✅ 验证根节点序号显示（[1], [2]）
- ✅ 验证子节点序号显示（[1.1], [1.2]）

### 2. 编辑器功能

#### 懒加载
- ✅ 展开节点后，应该看到 "Click to load editor..." 提示
- ✅ 点击提示区域，CodeMirror 编辑器应该加载
- ✅ 验证编辑器显示 Markdown 语法高亮

#### 编辑内容
- ✅ 在编辑器中输入内容
- ✅ 验证内容可以正常输入和修改
- ✅ 打开浏览器控制台，查看 `Tree changed` 日志

### 3. 运行功能

#### 测试运行按钮
- ✅ 点击任意节点的 "运行" 按钮
- ✅ 验证按钮显示 "Running..." 状态
- ✅ 1 秒后应该弹出提示框显示运行结果
- ✅ 打开控制台查看 `Run API called` 日志
- ✅ 验证 `onNodeRun` 回调被触发

#### 运行后状态
- ✅ 运行成功后，节点应该显示 "(已运行)" 标记
- ✅ 锁定按钮应该变为可用状态

### 4. 锁定功能

#### 锁定节点
- ✅ 运行节点后，点击 "🔒 锁定" 按钮
- ✅ 验证节点变为锁定状态（显示 🔒 图标）
- ✅ 验证编辑器变为只读模式
- ✅ 验证删除按钮被禁用
- ✅ 打开控制台查看 `Node lock callback` 日志

#### 解锁节点
- ✅ 点击 "🔓 解锁" 按钮
- ✅ 验证节点恢复可编辑状态
- ✅ 验证删除按钮恢复可用

### 5. AI 优化功能

#### 测试优化按钮
- ✅ 点击任意节点的 "AI 优化" 按钮
- ✅ 等待 1.5 秒（模拟 AI 处理时间）
- ✅ 验证编辑器内容被追加优化文本
- ✅ 打开控制台查看 `Optimize API called` 日志
- ✅ 验证 `onNodeOptimize` 回调被触发
- ✅ 查看控制台中的思考过程日志

### 6. 删除功能

#### 删除节点
- ✅ 点击任意节点的 "删除" 按钮
- ✅ 确认删除对话框弹出
- ✅ 点击确认
- ✅ 验证节点从树中消失
- ✅ 打开控制台查看 `Tree changed` 日志

---

## 🔍 预期行为检查清单

### 核心功能
- [ ] 树形结构正确显示（2 个根节点，1 个子节点）
- [ ] 节点序号正确（1, 1.1, 2）
- [ ] 展开/折叠功能正常
- [ ] 编辑器懒加载正常
- [ ] CodeMirror 语法高亮正常

### 运行功能
- [ ] 运行按钮点击后显示 loading
- [ ] 运行成功后弹出结果
- [ ] 运行后 hasRun 状态更新
- [ ] onNodeRun 回调触发

### 锁定功能
- [ ] 未运行时锁定按钮禁用
- [ ] 运行后锁定按钮可用
- [ ] 锁定后编辑器只读
- [ ] 锁定后删除按钮禁用
- [ ] 锁定图标显示正确

### AI 优化
- [ ] 优化按钮点击后正常工作
- [ ] 优化后内容更新
- [ ] 思考过程打印到控制台
- [ ] onNodeOptimize 回调触发

### 数据流
- [ ] 编辑内容时触发 onChange
- [ ] 删除节点时触发 onTreeChange
- [ ] 所有回调在控制台中可见

---

## 🐛 已知问题

### 待实现功能
1. ❌ 拖拽功能（需要集成拖拽库）
2. ❌ 依赖配置组件
3. ❌ 依赖循环检测
4. ❌ 依赖关系可视化

### 样式优化
1. ⚠️ 默认样式较简单，可以进一步优化
2. ⚠️ 响应式布局待完善

---

## 📊 测试步骤

### 快速测试（5 分钟）

1. **打开浏览器**
   - 访问 http://localhost:8002
   - 打开浏览器控制台（F12）

2. **基础功能测试**
   - 展开/折叠所有节点
   - 点击每个节点的 "运行" 按钮
   - 观察弹出框和控制台日志

3. **锁定功能测试**
   - 运行一个节点后锁定
   - 尝试编辑（应该被禁用）
   - 尝试删除（应该被禁用）

4. **AI 优化测试**
   - 点击 "AI 优化" 按钮
   - 等待优化完成
   - 验证内容更新

### 完整测试（15 分钟）

1. **遍历所有节点**
   - 测试每个节点的所有功能
   - 记录任何异常行为

2. **数据流测试**
   - 编辑多个节点的内容
   - 观察控制台的 Tree changed 日志
   - 验证数据结构正确

3. **状态转换测试**
   - 运行 → 锁定 → 解锁 → 编辑 → 删除
   - 验证每个状态转换正确

4. **边界情况**
   - 测试空内容节点
   - 测试深层嵌套节点（需要手动添加）
   - 测试大量节点（性能测试）

---

## 📝 测试报告模板

### 测试日期
YYYY-MM-DD

### 测试人员
[姓名]

### 测试结果

#### ✅ 通过的功能
- 功能 1
- 功能 2

#### ⚠️ 有问题但不阻断的功能
- 问题描述

#### ❌ 阻断性问题
- 问题描述

### 浏览器信息
- 浏览器：Chrome/Firefox/Safari
- 版本：xx.x.x

### 备注
[其他观察或建议]

---

## 🎉 成功标准

所有核心功能正常工作，包括：
- ✅ 树形结构显示和展开/折叠
- ✅ 编辑器懒加载和编辑
- ✅ 运行功能和回调
- ✅ 锁定/解锁机制
- ✅ AI 优化功能
- ✅ 删除功能
- ✅ 所有回调函数正确触发

---

**测试页面持续运行中**：http://localhost:8002  
**如有问题，请查看浏览器控制台和终端日志**
